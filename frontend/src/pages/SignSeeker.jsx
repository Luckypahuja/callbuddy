import { useEffect, useState } from "react"; import { getSignSeekerStatus } from "../services/api";
export default function SignSeeker({ navigate }) { const [status, setStatus] = useState("Checking availability…"); useEffect(() => { getSignSeekerStatus().then(x => setStatus(x.status === "coming_soon" ? "Coming soon" : "Unavailable")).catch(() => setStatus("Backend unavailable")); }, []); return <main className="page placeholder-page"><button className="back-button" onClick={() => navigate("/")}>← Back to agents</button><p className="eyebrow">SIGN SEEKER · {status}</p><h1>Communication without barriers.</h1><p className="lead-notice">Sign Seeker is a planned sign-language communication assistant. Camera input, sign recognition, text, and speech support will be introduced in a later phase.</p><div className="coming-soon"><span>⌁</span><div><h2>Coming soon</h2><p>The foundation is ready for an accessible, privacy-conscious communication experience.</p></div></div></main>; }
