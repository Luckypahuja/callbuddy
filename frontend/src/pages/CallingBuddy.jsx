import { useEffect, useRef, useState } from "react";
import AgoraRTC from "agora-rtc-sdk-ng";
import VoiceOrb from "../components/VoiceOrb";
import { startCallingBuddy, stopCallingBuddy } from "../services/api";
export default function CallingBuddy({ navigate }) { const [state,setState]=useState("idle"),[notice,setNotice]=useState("Ready to talk?"); const client=useRef(),track=useRef(),session=useRef(); const cleanup=async()=>{track.current?.close();track.current=null;if(client.current){await client.current.leave();client.current=null}};useEffect(()=>()=>{cleanup()},[]);const start=async()=>{try{setState("connecting");setNotice("Requesting microphone access…");track.current=await AgoraRTC.createMicrophoneAudioTrack();session.current=await startCallingBuddy();client.current=AgoraRTC.createClient({mode:"rtc",codec:"vp8"});client.current.on("user-published",async(user,type)=>{await client.current.subscribe(user,type);if(type==="audio"){user.audioTrack.play();setState("speaking")}});client.current.on("user-unpublished",()=>setState("listening"));await client.current.join(session.current.app_id,session.current.channel,session.current.rtc_token,session.current.user_uid);await client.current.publish([track.current]);setState("listening");setNotice("Listening… Speak naturally in English, Hindi, or Hinglish.")}catch(error){console.error(error);await cleanup();setState("error");setNotice(error?.name==="NotAllowedError"?"Microphone access was denied. Please allow it and try again.":"Unable to connect to Calling Buddy. Please try again.")}};const stop=async()=>{try{if(session.current?.session_id)await stopCallingBuddy(session.current.session_id)}catch(e){console.error(e)}finally{await cleanup();session.current=null;setState("ended");setNotice("Conversation ended.")}};return <main className="page buddy-page"><button className="back-button" onClick={()=>{stop();navigate("/")}}>← Back to agents</button><div className="buddy-header"><div><p className="eyebrow">CALLING BUDDY</p><h1>Your multilingual <em>voice companion.</em></h1></div><span className={`state-badge ${state}`}>{state}</span></div><section className="voice-session"><VoiceOrb state={state}/><p className="session-notice">{notice}</p><div className="language-row"><span>Selected language</span><strong>English / Hindi / Hinglish</strong></div><div className="session-actions">{["idle","ended","error"].includes(state)?<button className="primary-button" onClick={start}>Start conversation</button>:<button className="secondary-button" onClick={stop}>Stop conversation</button>}</div></section><section className="transcript"><div className="section-title"><h2>Live conversation</h2><span>Voice session</span></div><div className="transcript-row"><span>E</span><p>Hello! Welcome to Calling Buddy. Which language would you like to speak?</p></div><p className="transcript-hint">Transcript events will appear here when enabled in the Agora session.</p></section></main>}
