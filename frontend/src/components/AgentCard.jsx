export default function AgentCard({ title, description, action, accent, onClick }) { return <article className={`agent-card ${accent}`}><span className="card-number">{accent === "calling" ? "01" : accent === "doctor" ? "02" : "03"}</span><div className="agent-card-copy"><h2>{title}</h2><p>{description}</p></div><button className="card-action" onClick={onClick}>{action} <span>→</span></button></article>; }
