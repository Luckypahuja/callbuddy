const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || "http://127.0.0.1:8000";
async function request(path, options = {}) { const response = await fetch(`${API_BASE_URL}${path}`, { headers: { "Content-Type": "application/json", ...options.headers }, ...options }); if (!response.ok) { const body = await response.json().catch(() => ({})); throw new Error(body.detail || "Request failed"); } return response.json(); }
export const startCallingBuddy = () => request("/api/calling-buddy/start", { method: "POST" }); export const stopCallingBuddy = (sessionId) => request("/api/calling-buddy/stop", { method: "POST", body: JSON.stringify({ session_id: sessionId }) }); export const getAIDoctorStatus = () => request("/api/ai-doctor/status"); export const getSignSeekerStatus = () => request("/api/sign-seeker/status");
